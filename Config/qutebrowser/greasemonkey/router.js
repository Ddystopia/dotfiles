// ==UserScript==
// @name         YouTube Auto Skip Ads
// @version      1.0.1
// @description  Skip YouTube ads automatically
// @author       Ddystopia
// @match        *://192.168.1.1/*
// ==/UserScript==

// TODO: Mutation Observer

(() => {
  console.log("Started");
  // setInterval(() => (document.cookie = "session_id=; path=/"), 10);
  // document.addEventListener("keydown", (e) => {
  //   if (e.code === "Escape" || e.key === "p") {
  //     document.querySelector(".lb_close").click();
  //   }
  // });
})();
